import { ZegoLogger } from "zego-express-logger";
export declare class StreamVideoCenter {
    publishVideoStreamList: Array<{
        video: HTMLMediaElement;
        mediaStream: MediaStream;
    }>;
    playVideoStreamList: Array<{
        streamID: string;
        video: HTMLMediaElement;
        mediaStream: MediaStream;
    }>;
    screenShotStreamList: Array<{
        stream: MediaStream;
    }>;
    audio: HTMLMediaElement[];
    logger: ZegoLogger;
    constructor();
    checkPublishVideo(localVideo: HTMLMediaElement): {
        video: HTMLMediaElement;
        mediaStream: MediaStream;
    } | null;
    checkPlayVideo(localVideo: HTMLMediaElement): {
        streamID: string;
        video: HTMLMediaElement;
        mediaStream: MediaStream;
    } | null;
    removePublishVideo(localVideo: HTMLMediaElement): void;
    setStreamAudioOutput(localVideo: any, audioOutput: string): boolean;
    setPlayStreamAudioOutput(streamid: string, audioOutput: string): boolean;
}
